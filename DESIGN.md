# Design — 知盛天气 0.0.9

这是 0.0.9 的锁定设计系统。界面修改必须先满足数据真实性与可读性，天气氛围只用于表达观测事实，不承担装饰职责。

## Genre

Atmospheric technical utility：克制、技术化、终端感；不使用玻璃拟态、柔光晕染、背景模糊或无意义漂浮装饰。

## Structure

- 首页：Workbench。当前天气是唯一主读数，分钟降水、小时、逐日和遥测按 `01//` 顺序向下展开。
- 设置与配置：Long Document。每一步都有当前任务、可执行动作、成功判据与返回路径。
- 弹窗：Terminal Sheet。保留明确标题、关闭、返回、主操作与完整状态反馈。

## Color and type

- 沿用现有 Zhisheng 深色终端色板，不在天气氛围中引入大面积新底色。
- 橙色只标记当前时刻、警报或重要坐标；青色/薄荷色用于可操作状态与有效数据。
- 正文使用 `ZhishengReading`，数值、代码、坐标和状态使用 `ZhishengMono`。
- 保留项目特色的 `01//` 段落编号。

## Weather ambience

- 总语法：不是模拟自然景观，而是让气象终端用计算机语言解释天气；所有图元必须能追溯到网格、字符、数据流、扫描、矢量、缓存、丢包或校验错误等计算机意象。
- 无模糊：禁止径向渐变、软椭圆、全屏雾化蒙层和发光晕。
- 静态天气不启动逐帧循环；动态天气只使用一个 Canvas 和固定数量图元。
- 晴：启动网格、坐标标尺和日照采样脉冲；晴夜：像素星图、轨道点和低频扫描。
- 云：数据缓存块和等值线队列；阴：更密的数据分层，不做软云团。
- 雨：`0/1` 数据流；雨强决定列数、速度与拖尾，风向只改变字符流的倾斜。
- 雪：`*`、`·` 与十六进制碎片；雷暴：数据雨叠加故障扫描和短促电信号。
- 雾：低能见度扫描缺口与丢包断线；霾/沙尘：噪声点阵、校验错误和琥珀色数据侵蚀。
- 风：矢量箭头和流场；冰雹/冻雨：硬质数据块、冻结游标和结晶符号。
- 风、昼夜、温度作为修饰项，不覆盖主天气现象。
- 未知代码不猜成多云：不显示氛围，并保留原始代码用于诊断。

### 成功基准

- 0.0.8 原始数据雨是氛围层的视觉基准：单一隐喻、稳定相位、整屏节奏、低亮度背景和可辨识的运动个性。
- 新天气必须推导出自己的计算机隐喻，禁止用同一套通用粒子换颜色冒充不同天气。
- 雨/小雨直接复用 0.0.8 原组件；任何内部优化不得改变其列数、速度、相位、拖尾或透明度公式。

### 开发者氛围实验室

- 入口只在开发者模式下可用，设置页组件位置固定，开关前后不引发布局跳动。
- 实验室复用生产首页内容组件，顶部仅增加模拟信号矩阵、氛围档位和强度控制。
- 虚构天气数据只保存在预览页面的 Compose 状态中；不得进入 ViewModel、WeatherRepository、WeatherCache、CityRepository 或数据源偏好。
- 返回实验室后回到设置；退出页面立即丢弃模拟状态，主页城市、缓存、滚动位置和真实天气不变。

## Motion

- 只动画 `alpha`、位移、旋转和 Canvas 内部时间参数，不动画布局尺寸。
- 状态切换以 180–260ms 为主；氛围切换最多 240ms，不双重绘制高密度场景。
- 支持系统“减少动态效果”；静态场景完全静止。
- 每帧禁止创建大集合、Path、Brush 或随机数对象。

## Data contract

- 所有供应商代码先映射为统一的现象、强度、降水相态、阵性/雷暴/冻结修饰，再进入 UI。
- 分钟降水统一为 `mm/h` 后才能绘图、阈值判断和显示。
- 城市、天气和缓存必须共享同一城市标识；切换城市时不得展示上一城市的数据。
- 手动锁定数据源时不静默混入其他供应商；自动模式的补全必须显示分块来源。
- 无法识别的代码保留为 UNKNOWN，禁止静默回退到 CLOUDY。

## Handoff gate

交付前必须通过：供应商代码矩阵测试、分钟降水单位测试、城市切换测试、氛围分类测试、全部单元测试、Debug APK 构建、真机覆盖安装和首页滑动帧统计。
