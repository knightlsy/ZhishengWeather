# TIANQI WEATHER TERMINAL // CI
name: Build // ASSEMBLE

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
    inputs:
      variant:
        description: '构建变体 (publicRelease / debug)'
        required: false
        default: 'publicRelease'
        type: choice
        options:
          - publicRelease
          - debug

permissions:
  contents: read
  actions: write

jobs:
  assemble:
    name: Assemble ${{ github.event.inputs.variant || 'publicRelease' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout // 检出
        uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4

      - name: Unit tests // 单元测试
        run: ./gradlew testDebugUnitTest --no-daemon --stacktrace
        continue-on-error: true

      - name: Android Lint // 静态检查
        run: ./gradlew lintDebug --no-daemon --stacktrace

      - name: Assemble debug // 构建调试包
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Assemble public release // 构建公共发布包
        run: ./gradlew assemblePublicRelease --no-daemon --stacktrace

      - name: 上传 publicRelease APK
        if: ${{ github.event.inputs.variant != 'debug' }}
        uses: actions/upload-artifact@v4
        with:
          name: ZhishengWeather-publicRelease
          path: app/build/outputs/apk/public/release/*.apk
          if-no-files-found: error

      - name: 上传 debug APK
        if: ${{ github.event.inputs.variant == 'debug' }}
        uses: actions/upload-artifact@v4
        with:
          name: ZhishengWeather-debug
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error