# 枳生天气 0.0.3 测试报告（自动核对结果）

测试日期：2026-08-10
测试基线：`agent/v0.0.3-stability` 分支，基线 15 项单元测试全绿、Lint 0 Error

## 测试范围

- 通读全部 32 个 Kotlin 源文件（data / ui / widget / model）+ AndroidManifest + widget 布局 XML + themes/strings
- 跑 `testDebugUnitTest`、`lintDebug`、`assembleDebug` 三项自动检查
- 逐文件审查：崩溃风险（NPE / 越界 / 除零 / coerceIn 边界）、单位换算一致性、状态管理、并发安全、资源泄漏

## 发现并修复的 Bug（7 个）

### Bug A（高·崩溃）逐日温度条 coerceIn 下界超过上界
- **文件**：`HomeScreen.kt` DailySection
- **现象**：原内联写法 `(hi - lo).coerceIn(0.03f, 1f - lo)`，当某天低温归一化值 `lo > 0.97` 时，`1f - lo < 0.03f`，即 coerceIn 下界大于上界。`Float.coerceIn` 在 min > max 时抛 `IllegalArgumentException`，导致逐日预报区域整体崩溃。极端高温分布（当天低温接近全周最高温）可触发。
- **修复**：提取 `tempBarParams` 纯函数，用 `val maxW = (1f - lo).coerceAtLeast(0f); val minW = minOf(0.03f, maxW)` 保证下界 ≤ 上界，且条不溢出右边界。

### Bug B（中·NPE）Open-Meteo 补缺接口 body!! 非空断言
- **文件**：`OpenMeteoApi.kt`（fetch / fetchHourly / fetchDaily 三处）
- **现象**：`resp.body!!.string()`，响应体为 null 时 NPE。同仓库 `OpenMeteoSource` 用的是安全的 `?.`，此处不一致。
- **修复**：改 `resp.body?.string() ?: return@withContext null`。

### Bug C（中低·状态）ViewModel loading 状态在任务取消时不复位
- **文件**：`WeatherViewModel.kt` refresh
- **现象**：`_loading.value = false` 在 `fetchWeather` 之后，快速切城市取消旧任务时，旧任务的 finally 不执行，loading 可能卡 true。
- **修复**：改 try/finally，并用 `if (fetchJob === job)` 仅当自己仍是当前任务时才清 loading，避免旧任务误清新任务的 loading。

### Bug D（中·显示）昨日温差 ΔT 单位不一致
- **文件**：`HomeScreen.kt` YesterdayCard
- **现象**：高低温用 `Fmt.temp(high, unit)` 显示华氏度，但 `ΔT` 用原始摄氏度 `th.roundToInt() - it.roundToInt()` 算差。华氏度模式下 ΔT 显示摄氏度差（如 +5°），与高低温读数（如 77°/68°，差应为 9°）对不上。
- **修复**：提取 `tempDelta` 纯函数，按当前单位换算后再算差。

### Bug E（低·显示）分钟降水 take 数量与语义不符
- **文件**：`OpenMeteoSource.kt`
- **现象**：15 分钟粒度降水 `take(9)` = 135 分钟，但注释"2 小时(8 段)"、`precipSummary`"未来两小时"、PrecipCard 标签"+120min"都是 120 分钟。
- **修复**：改 `take(8)`（8 × 15 = 120 分钟）。

### Bug F（中·数据）小米能见度未按 unit 换算
- **文件**：`WeatherRepository.kt` mapXiaomiToWeatherData
- **现象**：`cur.visibility?.value?.toDoubleOrNull()` 直接用值，忽略 `unit` 字段。同文件小米风速按 unit 换算了，和风能见度也用 `distKm` 换算，唯独小米能见度没换算。若小米返回 `unit="m"`（米），会显示成 1000 倍（如 10000 km 而非 10 km）。
- **修复**：按 unit 换算（`"m" -> /1000.0`，km 或缺省透传）。

### Bug G（功能·设置无效）开机自检动画开关无人读取
- **文件**：`WeatherViewModel.kt` + `HomeScreen.kt`
- **现象**：`SettingsRepository.bootAnim` 在设置页有开关，但 `DisplayPrefs` 不含该字段，`BootState` 总是播放打字动画。用户关闭"开机自检动画"完全无效。
- **修复**：`DisplayPrefs` 加 `bootAnim` 字段，`displayPrefs` combine 加 `bootAnim` flow，`BootState` 接收参数并在 `false` 时直接显示全部行（不逐行打字延迟）。

## 新增单元测试（12 项）

文件：`app/src/test/kotlin/com/zhisheng/weather/ui/home/TempBarParamsTest.kt`

| 测试 | 覆盖 |
|---|---|
| `normalizesWithinUnitRange` | tempBarParams 正常归一 |
| `doesNotCrashWhenLowApproachesWeekMax` | lo > 0.97 不崩溃（Bug A 回归） |
| `doesNotCrashWhenLowEqualsWeekMax` | lo = 1.0 极端不崩溃 |
| `doesNotCrashWhenAllTemperaturesEqual` | 全周温度相同不除零 |
| `sortsSwappedHighLow` | 高低温倒挂排序 |
| `fahrenheitConversionMatchesCelsiusProportions` | 华氏度归一比例一致 |
| `nullValuesFallBackToWeekExtremes` | null 兜底 |
| `coerceInThrowsWhenMinimumExceedsMaximum` | 回归依据：确认 coerceIn min>max 抛异常 |
| `tempDeltaMatchesDisplayedUnit` | ΔT 按显示单位算（Bug D 回归） |
| `tempDeltaHandlesNegativeAndZero` | 负温差 / 零温差 |
| `tempDeltaReturnsNullOnMissingValues` | null 返回 null |

## 验证结果

最终自动检查（含全部 7 个修复 + 12 个新测试）：

```
./gradlew testDebugUnitTest lintDebug assembleDebug
BUILD SUCCESSFUL in 1m 55s
```

- `testDebugUnitTest`：27 项通过，0 失败（原 15 + 新 12）
- `lintDebug`：0 Error（未引入新 Error）
- `assembleDebug`：通过

## 修改文件清单

| 文件 | 改动 | 对应 Bug |
|---|---|---|
| `data/OpenMeteoApi.kt` | 3 处 `body!!` → `body?` | B |
| `data/OpenMeteoSource.kt` | `take(9)` → `take(8)` | E |
| `data/WeatherRepository.kt` | 小米 visibility 按 unit 换算 | F |
| `ui/WeatherViewModel.kt` | loading try/finally + bootAnim flow | C, G |
| `ui/home/HomeScreen.kt` | tempBarParams + tempDelta + BootState | A, D, G |
| `ui/home/TempBarParamsTest.kt` | 新增测试文件（12 项） | A, D 回归 |

## 未修的低优先级问题（记录，建议后续处理）

1. **LocationSource.xiaomiReverse 归属地顺序**：`affiliation` 用 `reversed()`，而 `CityRepository.search` 小米结果直接用原始字符串，定位与搜索得到的归属地格式/顺序可能不一致。需确认小米 API 实际返回格式后统一。
2. **QWeatherApi.lat() 命名误导**：`lat()` 实为通用坐标格式化（`%.2f`），第 75 行用它格式化经度 `lon = QWeatherApi.lat(longitude)`。功能正确，但易误导。建议加 `lon()` 别名。
3. **lastFetchedKey / lastFetchKey 双变量**：WeatherViewModel 两个几乎同名的变量职责不同但总同步，是维护隐患。功能正确，建议合并。
4. **逐时/逐日用系统时区而非城市时区**：`formatHour`/`formatWeekday` 用 `ZoneId.systemDefault()`，海外城市的逐时标签会偏移。主 App 与小组件一致，属设计选择，影响海外城市用户。
5. **pct 的 <=1.0 边界**：`if (it <= 1.0) it * 100.0 else it`，若和风返回 1.0 表示 1% 会被当比例变 100%。和风实际返回 0-100 整数，不会触发，低风险。
6. **OpenMeteoSource us_aqi 重复计算**：`a.us_aqi?.let { Math.round(it).toInt() }` 在 value 和 level 各算一次。轻微性能，功能正确。

## 构建环境备注

- aapt2 中文 SDK 路径乱码 warning（`Failed to stat file 'D:\...\android.jar'`），不影响构建。
- JDK 17（`D:\金川党建数据大屏项目\tools\jdk-17.0.13+11`）需通过 `JAVA_HOME` 指定，系统 PATH 无 java。
